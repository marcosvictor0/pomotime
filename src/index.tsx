import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// JSX = JS  = JS, X = XML

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
